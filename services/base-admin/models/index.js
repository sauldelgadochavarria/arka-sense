const Tenant = require('./tenant');
const TenantDomain = require('./tenantDomain');
const Menu = require('./menu');
const Plan = require('./plan');

module.exports = {
  Tenant,
  TenantDomain,
  Menu,
  Plan
};
